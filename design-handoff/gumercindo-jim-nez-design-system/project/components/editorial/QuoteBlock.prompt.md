The chapter epigraph box used through the workbooks.

```jsx
<QuoteBlock>
  "La pregunta no es si deberías salir del bar, sino cuándo darás ese primer paso."
</QuoteBlock>
```

Soft tint + gold left rule. For a big display pull-quote use `Testimonial` instead. Honors `--sheet-tint` (green on a workbook, violet in the diario).
